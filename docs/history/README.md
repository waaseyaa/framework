# docs/history — frozen implementation history

These trees are **point-in-time records of how the framework was built**. They are
*not* current documentation and are not kept in sync with the code. For enduring,
maintained knowledge, use `docs/specs/` (canonical living specs) instead.

| Tree | What it is | Status |
|------|-----------|--------|
| `plans/` | Dated design & implementation plans (session artifacts). Includes the Aurora-era → Waaseyaa rename design docs. | Frozen (last activity 2026-05-21) |
| `superpowers/` | The pre-Spec-Kitty planning workflow (`plans/`, `specs/`, `reviews/`). | Superseded 2026-04-24 by the repo-root `kitty-specs/` mission system |

Relocated from `docs/plans/` and `docs/superpowers/` so the canonical-vs-historical
boundary that `CLAUDE.md` describes is reflected in the directory layout. Nothing was
deleted — these remain an audit trail of decisions and their rationale.
