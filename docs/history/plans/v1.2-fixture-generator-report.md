# v1.2 Fixture Generator + Templates Report

Date: 2026-03-04  
Scope: `#136 v1.2: Fixture pack generator + scenario templates`

## Delivered

- Added deterministic fixture generation command:
  - `fixture:generate`
- Added built-in topology templates:
  - `fanout`
  - `chain`
  - `mixed-workflow`
- Generated scenario contract matches existing fixture-pack workflow shape:
  - `nodes` map
  - `relationships` list (stable key ordering)

## Determinism Controls

- Node keys and relationship keys are generated in stable lexical order.
- Relationship arrays are sorted by `key`.
- Timestamp progression is deterministic from fixed base timestamp input.

## Verification Evidence

- `./vendor/bin/phpunit --configuration phpunit.xml.dist packages/cli/tests/Unit/Command/FixtureGenerateCommandTest.php packages/cli/tests/Unit/Command/FixtureScaffoldCommandTest.php packages/cli/tests/Unit/Command/FixturePackRefreshCommandTest.php packages/foundation/tests/Unit/Kernel/ConsoleKernelTest.php`
  - PASS (11 tests, 29 assertions)

## Notes

- `fixture:generate` is wired into `ConsoleKernel` and visible in command listings.
