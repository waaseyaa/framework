# v1.4 Public Search UX Report

Date: 2026-03-04  
Scope: `#154 v1.4: Public search UX (semantic + graph hybrid)`

## Delivered (Minoo app workspace)

Updated Minoo public home SSR shell to provide product-level public search UX:

- File: `/home/jones/dev/minoo/templates/page.html.twig`
  - Added public search interface (desktop/mobile responsive)
  - Added client-side semantic query flow via `/api/search`
  - Added graph-context enrichment cues via `/api/discovery/hub/{type}/{id}` for top results
  - Added deterministic status/result rendering and URL query sync (`/search?q=...`)
- File: `/home/jones/dev/minoo/tests/Snapshots/ssr/public-home.snap.html`
  - Updated SSR snapshot to lock v1.4 public search shell markup

## Verification Evidence

Executed in `/home/jones/dev/minoo`:

- `WAASEYAA_UPDATE_SNAPSHOTS=1 php ./vendor/bin/phpunit --configuration phpunit.xml.dist --filter SsrSnapshotTest`
  - PASS (7 tests, 44 assertions)
- `php ./vendor/bin/phpunit --configuration phpunit.xml.dist --filter '/SsrSnapshotTest|SemanticSearchIntegrationTest/'`
  - PASS (9 tests, 53 assertions)

## Notes

- The local Minoo workspace in this environment is not a Git working tree (`.git` absent), so code changes are applied and verified locally but not committed from this session.
