# v1.0 Semantic Warming + Read-Path Baseline Report

Date: 2026-03-04  
Scope: `#119 v1.0: Semantic index warmers + read-path performance baselines`

## Delivered

- Added reusable warmer service: `SemanticIndexWarmer`.
- Added operational CLI command: `semantic:warm`.
- Added deterministic baseline drift gate:
  - `tests/Integration/Phase15/SemanticWarmBaselineIntegrationTest.php`
  - baseline hash: `7d1f94b39753d5ed3bd07028e35244f853198e26`

## Verification Evidence

- `./vendor/bin/phpunit --configuration phpunit.xml.dist packages/ai-vector/tests/Unit/SemanticIndexWarmerTest.php packages/cli/tests/Unit/Command/SemanticWarmCommandTest.php`
  - PASS (4 tests, 20 assertions)
- `./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Integration/Phase15/SemanticWarmBaselineIntegrationTest.php`
  - PASS (1 test, 7 assertions)
- `./vendor/bin/phpunit --configuration phpunit.xml.dist packages/foundation/tests/Unit/Kernel/ConsoleKernelTest.php`
  - PASS (5 tests, 7 assertions)

## Baseline Contract

Deterministic snapshot covers:

- warmer output (`processed/stored/removed` and per-type status),
- semantic search output ordering/shape (title-based stable contract snapshot),
- keyword fallback output ordering/shape,
- MCP `ai_discover` output ordering/shape and graph-context totals.

Latency budgets enforced by the drift gate:

- warm <= 5000ms
- semantic search <= 5000ms
- MCP ai_discover <= 5000ms

## Notes

- Snapshot intentionally avoids random JSON:API resource IDs (UUIDs) and uses stable title ordering to prevent false-positive drift.
- Command exit semantics are explicit: missing embedding provider returns non-zero to fail operational pipelines early.
