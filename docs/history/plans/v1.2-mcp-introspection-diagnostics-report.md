# v1.2 MCP Introspection and Diagnostics Tooling Report

Date: 2026-03-04  
Scope: `#139 v1.2: MCP introspection and diagnostics tooling`

## Delivered

- Added MCP introspection RPC surface:
  - `tools/introspect`
- Introspection payload includes deterministic diagnostics for:
  - contract metadata (`protocol_version`, `contract_version`, `contract_stability`, stable meta injection)
  - cache context (read-cache eligibility, scope, account context, key dimensions, tag hints)
  - visibility context (source visibility and workflow visibility policy hints)
  - permission boundaries and execution-path stages for each tool
  - root-cause failure mode hints per tool category
- Preserved existing MCP tool payload contracts for `tools/call` by limiting changes to additive introspection and shared canonical-tool normalization.

## Verification Evidence

- `./vendor/bin/phpunit --configuration phpunit.xml.dist packages/mcp/tests/Unit/McpControllerTest.php`
  - PASS (22 tests, 138 assertions)

## Notes

- `tools/introspect` supports legacy alias normalization (`search_teachings` -> `search_entities`) while exposing both requested and canonical tool names for debugging.
