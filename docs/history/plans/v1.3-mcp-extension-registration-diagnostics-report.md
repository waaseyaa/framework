# v1.3 MCP Extension Registration and Diagnostics Hooks Report

Date: 2026-03-04  
Scope: `#146 v1.3: MCP extension registration and diagnostics hooks`

## Delivered

- Added MCP extension registration surface on `McpController` via constructor-level `extensionRegistrations` contract.
- Added deterministic extension diagnostics to `tools/introspect` output:
  - applicable registered extensions,
  - extension hook declarations,
  - execution path hook markers (`extensions:<hook>`).
- Added canonical tool-name normalization for extension matching (`search_teachings` -> `search_entities`).
- Preserved existing `tools/call` payload contracts; extension diagnostics are additive and introspection-only.
- Added MCP unit coverage for extension registration/introspection behavior.
- Updated subsystem spec:
  - `docs/specs/mcp-endpoint.md`

## Registration Contract

Per-extension registration entry supports:

- `id` or `plugin_id`
- `label`
- `tools` (optional; empty means all tools)
- `hooks` (optional; defaults to `before_tool_call`, `after_tool_result_meta`)

## Verification Evidence

- `./vendor/bin/phpunit --configuration phpunit.xml.dist packages/mcp/tests/Unit/McpControllerTest.php`
  - PASS (23 tests, 142 assertions)
