# v1.4 Timeline Navigation Report

Date: 2026-03-04  
Scope: `#156 v1.4: Timeline navigation (temporal edges + date filters)`

## Delivered (Minoo app workspace)

Extended Minoo public discovery shell with timeline navigation and temporal filtering:

- File: `/home/jones/dev/minoo/templates/page.html.twig`
  - Added timeline panel driven by `/api/discovery/timeline/{entity_type}/{id}`
  - Added temporal filter controls (`from` and `to` date inputs)
  - Wired timeline loading to active anchor entity from topic-hub-enhanced search flow
  - Added deterministic timeline item rendering with date + direction + endpoint badges
- File: `/home/jones/dev/minoo/tests/Snapshots/ssr/public-home.snap.html`
  - Updated SSR snapshot for v1.4 timeline shell markup

## Verification Evidence

Executed in `/home/jones/dev/minoo`:

- `WAASEYAA_UPDATE_SNAPSHOTS=1 php ./vendor/bin/phpunit --configuration phpunit.xml.dist --filter SsrSnapshotTest`
  - PASS (7 tests, 44 assertions)
- `php ./vendor/bin/phpunit --configuration phpunit.xml.dist --filter '/SsrSnapshotTest|SemanticSearchIntegrationTest/'`
  - PASS (9 tests, 53 assertions)

## Notes

- The local Minoo directory in this environment is not a Git worktree (`.git` absent), so app code changes are applied and verified locally but not committed from this session.
