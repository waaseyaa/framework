# v1.6 Multi-Source Fixture Pack Report (#178)

## Goal
Expand the fixture corpus with federated multi-source scenarios; cover adapter normalization, dedupe, merge conflicts, and refresh deltas; ensure fixture-pack hash and replay determinism remain stable.

## Delivered
- **Spec:** Extended [docs/specs/ingestion-fixture-pack-contract.md](../specs/ingestion-fixture-pack-contract.md) with v1.6 Multi-Source Fixture Pack section (#178):
  - Canonical paths for multi-source ingestion fixtures (adapter-normalization-*.json, multi-source-federated.input.json, multi-source-refresh.*.json) and connector fixtures (dataset, api, file, crawl).
  - Required coverage matrix: adapter normalization, dedupe/identity, merge conflicts, refresh deltas.
  - Regression consumption: SourceAdapterNormalizerTest, MultiSourceFixturePackTest, IngestionFixturePackRegressionTest, FixturePackRefreshCommandTest.
  - Determinism rules aligned with v1.5.
- **Fixtures:** Existing v1.6 fixture set documented and contract-stable:
  - Adapter normalization: `tests/fixtures/ingestion/adapter-normalization-canonical.json`, `adapter-normalization-mutation.json`.
  - Federated identity/merge: `tests/fixtures/ingestion/multi-source-federated.input.json`.
  - Refresh deltas: `tests/fixtures/ingestion/multi-source-refresh.baseline.json`, `multi-source-refresh.current.json`.
  - Connectors: `tests/fixtures/connectors/dataset.json`, `api.json`, `file.json`, `crawl.json`.
- **Regression:** Added `MultiSourceFixturePackTest::v16_fixture_pack_paths_exist_and_are_valid_json()` so the regression suite asserts all v1.6 fixture paths exist and are valid JSON; existing test continues to assert identity stability, merge diagnostics (`merge.field_conflict`), and refresh category (`refresh.relationship_change`). SourceAdapterNormalizerTest consumes adapter-normalization fixtures; IngestionFixturePackRegressionTest and FixturePackRefreshCommandTest cover scenario pack and hash stability.

## Verification
- `./vendor/bin/phpunit --configuration phpunit.xml.dist packages/cli/tests/Unit/Ingestion/MultiSourceFixturePackTest.php packages/cli/tests/Unit/Ingestion/SourceAdapterNormalizerTest.php`
  - PASS (2 tests in MultiSourceFixturePackTest, 3 in SourceAdapterNormalizerTest; v1.6 paths and federated identity/merge/refresh covered)

## Acceptance Mapping
- Fixture corpus deterministic and versioned: PASS (static, version-controlled paths; determinism rules in contract).
- Federated edge cases covered (adapter normalization, dedupe, merge conflicts, refresh deltas): PASS.
- Regression suites consume expanded fixture pack consistently: PASS.
