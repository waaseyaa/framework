# v1.2 Performance Tooling CLI Report

Date: 2026-03-04  
Scope: `#138 v1.2: Performance tooling CLI (baseline + compare)`

## Delivered

- Added `perf:baseline` command:
  - generates versioned baseline artifacts with deterministic `thresholds_ms` ordering.
- Added `perf:compare` command:
  - compares current measurement artifacts against baseline snapshot hash + thresholds,
  - returns explicit drift violations and non-zero exit on regression.
- Wired both commands into `ConsoleKernel` command catalog.

## Artifact Contracts

- Baseline contract:
  - `contract_version`
  - `surface`
  - `snapshot_hash`
  - `thresholds_ms`
- Current measurement contract (for compare):
  - `snapshot_hash`
  - `durations_ms`

## Verification Evidence

- `./vendor/bin/phpunit --configuration phpunit.xml.dist packages/cli/tests/Unit/Command/Perf/PerformanceBaselineCommandTest.php packages/cli/tests/Unit/Command/Perf/PerformanceCompareCommandTest.php packages/foundation/tests/Unit/Kernel/ConsoleKernelTest.php`
  - PASS (9 tests, 22 assertions)
