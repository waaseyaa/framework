# FW-GENERATION-UNITS-07 — dormant additive successor evolution

Date: 2026-09-04. Forge mirror: #2846. Authority: ADR-025 D-2.3a and D-12.1 slice 7.
Accepted publication base: 6a9f87278cab54aee1f764afe54c874896eaab56, containing slice 6 (#2887), its delivery evidence (#2888), and the separately owned #2836/#2870/#2780 fixes. Only this slice's commit was replayed from the original local slice-6 candidate base; source changes from that candidate were not duplicated.

## Contract

Replace the dormant blanket additive refusal with a closed eligibility declaration for exactly the managed root site binding using SiteArtifactRenderer. The manifest compiler is the sole current binding; the future approved-blueprint compiler is explicitly not admitted. The seeded compiler allowlist stays empty. No handler, public controlled apply or legacy reader/report/journal behavior changes; slice 8 alone activates the engine and stale-plan enforcement.

An additive declaration from any other unit/compiler/disposition refuses GEN011 before it can acquire authority, even when its path set does not change. Existing root compiler/manifest/input identity checks remain mandatory. This is a closed compiler binding, not metadata authentication or an extension mechanism.

For existing supplied units compute sorted adds/drops against their recorded paths. Frozen additions refuse GEN011; frozen drops retain GEN009. Eligible additive drops refuse GEN011; eligible additions pass through the existing collision, containment, cross-owner, managed-byte, extension-region and transaction checks without a bypass. A first publish reports empty adds/drops. A same-input managed consistency check must only compare carried paths; new paths have no prior row, and an undefined-index warning is not a refusal policy.

No ProjectStateIdentity, ArtifactPlan, EvaluatedArtifactPlan, GeneratedSite, GeneratedArtifact or renderer shape changes are required. Existing project-state observation already includes the union of requested and supplied/retired recorded paths. Preserve the existing metadata-last publisher and registration effects. No second reader, publisher, lock, collision authority or digest.

Root additions must be exercised using a real changed manifest and SiteArtifactRenderer output. Unit-aware doctor must agree with the newly composed renderer projection after publication. Legacy initialize continues its unconditional frozen-set behavior until activation; update the enduring spec with the named dormant exception and activation boundary, without claiming a live behavior change.

## Ownership and verification

The cheaper implementation worker owns SiteInitializationService.php and new GenerationAdditiveEvolutionTest.php only. It may update only the obsolete additive-refusal assertion in GenerationUnitEngineTest.php after coordinating the exact expectation. Primary owns this record, specs, release fragment and closed-eligibility architecture tests, integration review and GitHub custody. No worker commits, pushes or edits another worktree.

Capture red tests first: real root growth, sorted delta, first publish, unchanged additive plan, frozen additions/drops, additive drops, every ineligible tuple (including unchanged/first publish), foreign/path-owner collision, modified carried content/extension handling, metadata-last rollback, and unchanged legacy behavior. Independently challenge eligibility and delta guards with deliberate rivals after source freeze. Final preflight, split suites, coverage and exact-head hosted checks govern publication.

## Review evidence

The implementation worker captured the red matrix before source changes. One legacy-message expectation in that initial fixture was wrong and was corrected against the unchanged legacy source; it is not a production defect. The final additive matrix passes 20 tests / 95 assertions, including first and existing unchanged ineligible tuples. Related engine, registration and doctor controls pass 141 tests / 547 assertions.

Independent source review and a separate 20-case control found no remaining blocking admission defect. The compiler name is a claimed binding and does not authenticate arbitrary plan contents; the concrete successor proof uses a real changed manifest and renderer output, then passes unit-aware doctor. The legacy wrong-root-binding code is GEN006; an initial reviewer recall of GEN001 was corrected against executable evidence.

Seven worker rivals were detected. Removing the non-root eligibility and frozen-addition guards can admit unauthorized plans; those are admission failures. Wrong-compiler/disposition, drop and unowned-collision rivals retain downstream refusal but violate the required code/precedence, so those results establish coded-policy sensitivity rather than an unsafe overwrite. The same-input rival exposes the missing-prior-row access. Source SHA-256 before and after rivals matched 6b78e9eefecadde01b9f1786506729aea6bb89965ca76949cd90875ef79830cb.

Primary's separate architecture rival appended an unapproved compiler to ADDITIVE_COMPILERS and failed the exact-list test. Restored staged/unit boundaries pass 7 tests / 105 assertions. The seeded list remains empty and no command activation is included.

Local qualification before base reconciliation passed full preflight 41/41, Unit 14,221 tests / 240,516 assertions, and focused changed-line coverage 17/17 executable lines (100%). The branch is reconciled to the accepted base above. Final committed-head preflight, split suites, coverage and hosted CI are recorded in the publication evidence; the earlier counts are not evidence for a later head.
