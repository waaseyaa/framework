# v1.1 Performance Fixture Expansion Report

Date: 2026-03-04  
Scope: `#131 v1.1: Performance fixture expansion (large graph + workflow scenarios)`

## Delivered

- Expanded deterministic fixture corpus in `tests/Support/WorkflowFixturePack.php` with v1.1 performance scenarios:
  - `performanceNodesLargeGraph()` (48 deterministic nodes with mixed workflow states)
  - `performanceRelationshipsLargeGraph()` (high-fanout + chain graph edges)
  - `performanceTraversalScenarios()` (fanout stress scenario set)
  - `performanceCacheInvalidationScenarios()` (mutation scenarios for invalidation coverage)
- Extended `corpusSnapshot()` to include all new performance fixture sets.
- Updated corpus hash gate in `WorkflowFixturePackIntegrationTest` to new deterministic snapshot hash.

## Determinism + Coverage

- Node corpus includes deterministic distribution across:
  - `published`
  - `review`
  - `draft`
  - `archived`
- Relationship corpus includes:
  - anchor fanout edges
  - mixed status edges
  - temporal and non-temporal edges
  - chain fanout for traversal depth/fanout stress
- Cache invalidation scenarios reference concrete deterministic fixture keys.

## Perf-Suite Consumption

- Added integration suite:
  - `tests/Integration/Phase15/PerformanceFixturePackIntegrationTest.php`
- Suite consumes performance fixtures directly and validates:
  - deterministic mixed-workflow large-graph generation,
  - high-fanout discovery topic-hub read scenarios,
  - cache-invalidation scenario referential integrity.

## Verification Evidence

- `./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Integration/Phase14/WorkflowFixturePackIntegrationTest.php tests/Integration/Phase15/PerformanceFixturePackIntegrationTest.php tests/Integration/Phase14/DiscoveryFixtureConsumersIntegrationTest.php`
  - PASS (9 tests, 54 assertions)

## Notes

- Relationship spec updated with the new performance fixture families:
  - `docs/specs/relationship-modeling.md`
- Deterministic corpus hash gate remains enforced through `WorkflowFixturePackIntegrationTest`.
