# v1.1 Performance Regression Gate Report

Date: 2026-03-04  
Scope: `#132 v1.1: Performance regression gate + baseline drift checks`

## Delivered

- Upgraded Phase15 baseline gate in:
  - `tests/Integration/Phase15/SemanticWarmBaselineIntegrationTest.php`
- Added versioned baseline artifact:
  - `tests/Baselines/performance_regression_v1.1.json`
- Expanded gate coverage to include explicit read-path surfaces:
  - semantic warm
  - semantic search
  - SSR relationship-navigation browse path
  - discovery topic-hub path
  - MCP `ai_discover`

## Baseline Contract

Artifact schema (`v1.1`):

- `contract_version`
- `surface`
- `snapshot_hash`
- `thresholds_ms` per surface

Drift behavior:

- snapshot hash mismatch fails with explicit expected/actual values,
- latency threshold breach fails with explicit surface + measured/threshold values.

## Local/CI Repeatability

- Default mode: validates against committed baseline artifact.
- Refresh mode (intentional baseline update):
  - `WAASEYAA_UPDATE_PERF_BASELINE=1 ./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Integration/Phase15/SemanticWarmBaselineIntegrationTest.php`
- This allows controlled baseline regeneration while preserving strict drift checks in normal CI runs.

## Verification Evidence

- `WAASEYAA_UPDATE_PERF_BASELINE=1 ./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Integration/Phase15/SemanticWarmBaselineIntegrationTest.php`
  - PASS (1 test, 18 assertions)
- `./vendor/bin/phpunit --configuration phpunit.xml.dist tests/Integration/Phase15/SemanticWarmBaselineIntegrationTest.php`
  - PASS (1 test, 18 assertions)

## Notes

- Spec updated:
  - `docs/specs/ai-integration.md` (baseline artifact + expanded surface/threshold contract)
