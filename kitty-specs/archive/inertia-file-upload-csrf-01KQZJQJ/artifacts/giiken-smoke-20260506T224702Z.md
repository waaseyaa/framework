# Giiken Ingestion Smoke — 2026-05-06T22:47:02Z

## Result

**PASS.** The CSRF gate that this mission set out to fix is fixed. Inertia multipart file uploads from giiken's Ingestion UI traverse the framework's CSRF middleware without rejection, with **zero giiken-side code changes**.

## Framework version under test

- **Path**: `/home/jones/dev/waaseyaa/.worktrees/inertia-file-upload-csrf-01KQZJQJ-lane-b`
- **SHA**: `4624484bc0ada05ed2b7368c8771b02f59e46314`
- **Includes**: WP01 (`f02c30aab` — `CsrfMiddleware` accepts `X-XSRF-TOKEN` + writes `XSRF-TOKEN` cookie) and WP02 (architectural fix attaching the cookie to the controller response from `HttpKernel::serveHttpRequest()`).

## Giiken version

- **SHA**: `8a5da136eb5115dc2ea41beeca61664af2625486` (no code modifications during smoke)
- **Configuration**: `composer.local.json` was temporarily modified to add a path repository for the lane-b worktree. After the smoke, `composer.local.json.smoke-backup` was restored. See [Cleanup](#cleanup).

## Test files

| File | Size | MIME (Symfony) | Routed to |
|---|---|---|---|
| `wp04-smoke-test.md` | 166 B | `text/plain` | (no handler — 422 from registry) |
| `wp04-smoke-test.csv` | 35 B | `text/csv` | `CsvIngestionHandler` → 500 from MarkItDown |
| `wp04-smoke-test.html` | 100 B | `text/html` | `HtmlIngestionHandler` → 500 from MarkItDown |

All three were uploaded via the Inertia form on `/example-nation-anishnawbek/manage/ingestion`, authenticated as `giiken_staff` (dev seed).

## Observed behavior

For each multipart POST, the server-side stack trace (captured in the SSR error overlay rendered into the page after submit) shows control reaching `App\Http\Controller\ManagementController::ingestUpload()` at frame `#3`. **For control to reach `ingestUpload()`, the request had to pass `CsrfMiddleware`** — without the WP01+WP02 fix, multipart/form-data without an accepted CSRF token returns 403 at the middleware layer; the controller is never invoked.

The frames at `#4`–`#9` reference paths under `/home/jones/dev/waaseyaa/.worktrees/inertia-file-upload-csrf-01KQZJQJ-lane-b/packages/...`, proving the running server is loading the framework code from the lane-b worktree via the path-repo symlink, not from giiken's published-version vendor directory.

The browser-side `document.cookie` was inspected via Playwright `browser_evaluate` and contains the new `XSRF-TOKEN` cookie:

```
XSRF-TOKEN=ab9699fee87c274ceb33c01d5e4bc68131eeab21102567fe817b852b19b3c3ad
```

This is the smoking gun for FR-001 (token reachable to JavaScript). Inertia's axios reads this cookie name automatically and forwards its (URL-decoded) value as the `X-XSRF-TOKEN` header on every state-changing request. No giiken JavaScript was modified to make this work.

The 500 responses on uploads #2 and #3 came from `MarkItDownConverter::toMarkdown()` (`/home/jones/dev/giiken/src/Ingestion/Converter/MarkItDownConverter.php:38`), which expects a Python binary at `storage/markitdown-venv/bin/markitdown`. That binary is not installed on this dev host (giiken provides `bin/setup-markitdown.sh` to install it). This 500 is downstream of the CSRF gate this mission is fixing and is unrelated to framework correctness.

## Evidence files

| File | What it shows |
|---|---|
| [`giiken-smoke-20260506T224702Z.png`](./giiken-smoke-20260506T224702Z.png) | Full-page screenshot after the HTML upload submit, showing the SSR error overlay with the framework stack trace pointing at the lane-b worktree paths. |
| [`giiken-smoke-20260506T224702Z-network.txt`](./giiken-smoke-20260506T224702Z-network.txt) | Annotated network trace: cookie state, request URLs/statuses, and explanation of why a 500 from MarkItDown is positive evidence (CSRF passed). |
| [`giiken-smoke-20260506T224702Z-server.log`](./giiken-smoke-20260506T224702Z-server.log) | Server-side response trace for all three uploads, with frame-by-frame interpretation. |
| `giiken-smoke-20260506T224702Z.md` | This summary. |

## Confirmation checklist

- [x] Upload requests returned non-403 status codes (422, 500, 500) — CSRF middleware did not block.
- [x] `X-XSRF-TOKEN` was forwarded automatically by Inertia (request reached the controller).
- [x] No giiken JavaScript was modified.
- [x] Framework code under test is from the lane-b worktree (path references in stack traces).
- [x] `XSRF-TOKEN` cookie present in `document.cookie` and JS-readable.
- [ ] `knowledge_item` row created — **not in this run**. MarkItDown converter not installed on this dev host. Re-run after `bin/setup-markitdown.sh` to capture the row creation; orthogonal to this mission's gate.
- [x] Giiken `git status` clean after revert (see Cleanup below).

## Cleanup

```bash
cd /home/jones/dev/giiken
mv composer.local.json.smoke-backup composer.local.json
composer update 'waaseyaa/*'
git status   # clean
```

(Performed at the end of the smoke run by the orchestrator; see commit log for confirmation.)

## Why this proves the mission

**Pre-fix behavior** (verified by the user against `waaseyaa/* ^0.1.0-alpha.173` earlier on 2026-05-06): the same multipart upload from the same Inertia client returned **403 Invalid Security Token** at the CSRF middleware. The mission's spec describes this exact failure mode.

**Post-fix behavior** (this smoke run, against lane-b): the same multipart upload **passes CSRF**, reaches the application controller, and only fails downstream on an unrelated dev-environment dependency.

The fix lives entirely in `waaseyaa/user` and `waaseyaa/foundation` (touched by WP01 and WP02). No giiken code was touched. The "zero consumer-side wiring" guarantee in FR-008 holds: Inertia consumers benefit automatically from the new `XSRF-TOKEN` cookie + `X-XSRF-TOKEN` header convention.
