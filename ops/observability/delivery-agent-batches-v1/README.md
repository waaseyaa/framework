# Delivery agent event batches (v1)

Immutable JSON envelopes live here as `<batch_id>.json`.
Publication is live only when gate enforcement and the #2869 projection reader are ready together.
See `docs/specs/delivery-agent-event-batches.md`.
