- Added the reserved `GEN0xx` refusal vocabulary for the generation execution and plan boundary: `GenerationErrorCode` enumerates the fifteen ids ADR-025 D-5 reserves, and `GenerationViolation`/`GenerationRefusalException` carry one coded refusal in the shape the plan result envelope uses. The family is coded but raised from no path yet; the slices that own each condition wire it up (#2846, ADR-025 D-5).
