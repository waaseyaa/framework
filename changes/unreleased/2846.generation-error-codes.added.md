- Added the reserved `GEN0xx` refusal vocabulary for the generation execution and plan boundary: `GenerationErrorCode` is checked directly against the closed ADR-025 D-5 table, and `GenerationViolation`/`GenerationRefusalException` carry coded refusals in the list shape the plan result envelope uses. Sparse or wrongly typed violation lists are refused rather than serialized incorrectly, and declared pointers must use JSON Pointer syntax including the root pointer. The family is coded but raised from no path yet; a slice-specific architecture proof records that boundary without growing the shrink-only dead-code baseline, and the slices that own each condition wire it up (#2846, ADR-025 D-5).
