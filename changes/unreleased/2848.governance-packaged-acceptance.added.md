- Added a bounded packaged-runtime acceptance harness (`tests/PackagedForm/check-blueprint-governance-enforcement`) for the compiled `application_blueprint` governance surface (#2848): installed-provenance and no-symlink checks, an unknown-metadata dry-run refusal with byte-identical whole-tree digest, a real installed-API decision receipt, literal-root provider registration, and the `config:import`/kernel-boot enforcement chain. The lane surfaced an honest RED — `config:import` rejects the compiler's own golden `workflows.assignments.yml` for a missing `_meta` block — documented with exact command/output and the smallest likely repair in `docs/change-records/FW-GOVERNANCE-PACKAGED-ACCEPTANCE-01.md`; no runtime was edited to force a pass.
