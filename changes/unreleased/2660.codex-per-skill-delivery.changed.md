- **Changed — Codex now receives per-skill Agent Skill delivery matching Claude, as the accepted implementation choice (#2660 Part B):** `ClientCapabilityRegistry` moves `codex` from `SingleConsolidatedFile` to `PerSkillFile`, backed by a verified upstream citation on `CodexClientTransformer` for `.agents/skills/` discovery (`https://learn.chatgpt.com/docs/build-skills#where-codex-loads-local-skills`, verified 2026-09-05). Codex now writes a concise root `AGENTS.md` index plus one `.agents/skills/waaseyaa-<id>/SKILL.md` per canonical skill, sharing the new `Waaseyaa\Bimaaji\Install\Client\AbstractPerSkillClientTransformer` with `ClaudeClientTransformer` (which shrinks to a thin subclass — no rendering behaviour change beyond the provenance addition below). Every per-skill file now carries a one-line provenance footer naming `SkillInventory::inventorySha256()`, and every guidance index lists each skill's own *source* sha256 (`ParsedSkill::$sourceSha256`, threaded through by `SkillSetParser`), so Claude and Codex per-skill output is provably byte-identical for the same canonical inventory. The new `ClientCapabilitySurface` enum and `ClientCapabilityDiagnostics::warnings()` give `bimaaji:install` a deterministic, warning-severity (exit 0) diagnostic when `--features` requests `skills` against a single-file client or `mcp_configuration`/`mcp` against any client, wired into `BimaajiInstallCommand`. Manifest ownership, `--dry-run`, and stale-target retirement needed no changes — they already operate generically on a transformer's declared `targetFiles()` paths. `docs/adr/026-client-guidance-and-skill-conventions.md` records the accepted technical decisions under the user's delegated delivery authority; candidate qualification and governed landing remain separate. The existing `tests/PackagedForm/check-bimaaji-skill-resources` proof now installs copied package bytes from the exact candidate and verifies Claude/Codex skill-id, source-hash, and per-skill-byte parity without a monorepo skills fallback; it also bounds generated root `AGENTS.md` at 16 KiB. `CodexClientTransformerTest` supplies the matching unit-level parity proof.

  The packaged proof now also covers the development-tool lifecycle: a minimal copied-package consumer keeps `waaseyaa/core` and `waaseyaa/cli` at runtime, installs the canonical `waaseyaa/ai-development` bundle through `require-dev`, resolves each skill's referenced `bin/waaseyaa` command from the installed catalogue, executes the real Bimaaji sync command, detects manifest-hash drift, and proves a marker-bounded update keeps human-authored content. Literal `composer install --no-dev` then removes both the development bundle and Bimaaji from the installed graph without deleting portable generated guidance or provenance. Generated-output reconciliation remains #2664.
