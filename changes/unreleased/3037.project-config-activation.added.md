- **Added — runnable generated applications (#3037):** authoring hosts can sign the exact configuration bytes from a canonical application-blueprint plan before initialization, and opt-in `project:init` now verifies and activates that bundle on a fresh trusted consumer with deterministic retry reconciliation and phase-aware JSON evidence.
