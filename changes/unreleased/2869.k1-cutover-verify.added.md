- **Added — read-only K1 cutover verifier (#2869):** `php bin/verify-k1-delivery-cutover` compares Panel 8 query results with projection identity, fails on missing provenance or mismatched source SHA, projector version, generation, event count, and hashes, and never prints connection credentials.
