- **Added -- receipt-bound K1 cutover verifier (#2869):** `php bin/verify-k1-delivery-cutover --receipt=...` compares the actual loopback Grafana Panel 8 result and query-only projection identity with an accepted projector receipt, requires the deployed SQL and datasource UID to match the tracked dashboard, and fails closed without printing credentials.
