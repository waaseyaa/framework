- Added `bin/check-ci-roster-conformance`, an offline verifier that compares the governed CI policy with the generated workflow inventory across twenty-nine rules covering bindings, visible contexts, matrix widths, selectors, dispositions, aggregate lineage, artifact contracts and cadence-to-trigger agreement, reporting errors, coverage notices, and explicit not-verified-offline lines for checks that need live ruleset state, with both it and the inventory-drift check added to the governed preflight roster.
