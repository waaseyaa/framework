- **Added — spec corpus compiler pilot (#2661, `FW-SPEC-CORPUS-01`):** `bin/compile-spec-corpus` and `tools/lib/SpecCorpus/*` compile a bounded pilot manifest of `docs/specs/` into a sanitized, versioned agent corpus with lifecycle metadata (`live`, `superseded`, `historical`, `draft`), a live-only default index, provenance-preserving retrieval text, deterministic chunks, fail-closed id/path/supersession validation, staging-first publication, and a `corpus_digest` that binds framework and document metadata.
