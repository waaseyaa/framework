- `bin/qualify-candidate` now runs its mandatory preflight alone and leaves the expensive suites explicitly `unrun` when that gate fails. Passing preflight still collects every suite result, while `--collect-all` provides an explicit diagnostic override without weakening qualification requirements.
