- **Fixed — changed-line coverage gate reports covered, uncovered, uninstrumented, and CoversNothing excluded-attribution boundaries without moving the 80% threshold (#2904):** operators can see when integration `#[CoversNothing]` metadata or clover-absent paths leave the percentage incomplete, instead of treating shard execution as attributed coverage.
