- **Added — `mcp:registry-manifest` emits official Registry `server.json` (#2638, `FW-MCP-REGISTRY-MANIFEST-01`):** a Layer-6 CLI command, gated on `waaseyaa/mcp`, writes the existing `McpRegistryManifest` JSON to stdout so a deployment can inspect or submit the artifact by hand. It does not publish to the Registry.
