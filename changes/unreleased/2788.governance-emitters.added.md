- **Added — blueprint governance compilation: permissions, access policies, workflow definitions, roles, and companion tests (#2788, FW-SITE-BLUEPRINT-01E):** `ApplicationBlueprintCompilerFactory::create()` composes five new pure emitters onto the FW-SITE-BLUEPRINT-01D roster without editing `ApplicationBlueprintCompiler`: `PermissionCatalogueEmitter` emits `src/Access/ApplicationBlueprintPermissions.php` (`PERMISSION_<UPPER_SNAKE>` constants, a `seed()`, and a `register()` mirroring `AgentCapabilities::register()`); `AccessPolicyEmitter` emits one open-by-default `src/Access/<Entity>Policy.php` per entity declaring at least one policy, translating each declared `permission`/`ownership`/`workflow_state` condition into `AccessResult::allowed()`/`::neutral()` (never `Forbidden`, never inspecting roles) and reading the owner/`workflow_state` inputs through the new `Waaseyaa\Access\Read\AuthorizationInputReader`; `WorkflowDefinitionEmitter` emits one `src/Workflow/<Workflow>WorkflowDefinition.php` (a real `Workflow::__construct()`-shaped `DEFINITION` constant) per blueprint workflow plus the aggregate `config/sync/workflows.assignments.yml`; `GovernanceProviderEmitter` emits a second, distinct `src/Provider/ApplicationBlueprintGovernanceServiceProvider.php` implementing `ProvidesRolesInterface` and seeding every declared workflow in `boot()` (mirroring `WorkflowServiceProvider`'s `[S1-DB106]`/`enforceIsNew()`/log-and-skip/additive-top-up seed pattern); and `GovernanceCheckEmitter` emits the blueprint's behavioural companion tests under `tests/Blueprint/` — `GovernanceDefaultDenyTest.php` unconditionally (every declared entity must deny an anonymous and a permission-less principal on every operation, even with zero declared policies) plus `RolePermissionChecksTest.php`/`EntityAccessChecksTest.php`/`WorkflowTransitionChecksTest.php` only when the blueprint declares a check of that kind, all reported as `companionTests`. `EntityClassEmitter` gains three companion rules: every ordinary and relationship field is sealed `read: FieldReadLevel::Public`; the `keys.owner` relationship field is sealed `Protected` with `settings.authorizationInput`; a workflow-bound entity also gains a generated `workflow_state` field byte-for-byte matching `Node::$workflow_state`. A blueprint policy's `ownership`/`workflow_state` condition on the `create` operation, and a blueprint role id of `administrator`, are both refused `GEN007_UNSUPPORTED_DECLARATION` at compile time, before any artifact is emitted; two permissions colliding on the same generated constant name are refused `GEN006_MALICIOUS_IDENTIFIER`. Verified end-to-end: all five governance emitters' output for the `complete.yaml` fixture, materialized together with the 01D-1 roster, executes as a real `proc_open` PHPUnit run and passes, including a `TransitionService` wired against a `TemporarySqliteDatabase`-backed repository. Fixture materialization and `fixture_present` checks remain 01D-3, out of scope here; the blueprint contract still has no per-field `read_level` an author can widen or narrow.
