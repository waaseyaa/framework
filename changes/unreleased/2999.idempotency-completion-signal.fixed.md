- **Fixed — idempotency completion failure masking (#2999, T-9 slice):** `IdempotencyStore::execute()` now rethrows `TransactionCompletionException` after commit without calling `rollBack()` on an inactive transaction, preserving durable replay rows and the original completion signal; pre-commit failures still roll back.
