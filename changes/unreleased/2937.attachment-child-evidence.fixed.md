- **Fixed — SaveActiveConcurrencyTest fork diagnostics and parent SQLite release (#2937):** Hosted `ci/test-shard-3` reported `Failed asserting that actual size 21 matches expected size 30` with no child stage/class/message because children used `catch (\Throwable) { exit(1); }` and the parent ignored `pcntl_waitpid()` exit status. The repair keeps all 30-process / 30-row / 1-active assertions intact, adds bounded per-child stage/class/message reports via `ForkChildDiagnostics` (EINTR-safe wait/reap, UTF-8-safe message bounding, invalid-UTF-8 substitution at strict JSON encode, reap already-launched children on partial fork failure), verifies every child exited normally before row assertions, and explicitly closes plus unsets the setup `AttachmentSchema` database reference before forking. Hosted child exception and root cause remain UNKNOWN. Focused regression coverage lives in `SaveActiveConcurrencyDiagnosticsTest` and `ForkChildDiagnosticsTest`.
