- **Fixed — blueprint emitters are pure functions of the canonical blueprint, proven by a complete-blueprint console flow (#2788, FW-SITE-BLUEPRINT-01E):** `SiteBlueprintProcessTest` gains a separate flow that drives the governance-rich `complete.yaml` through the real `site:init --dry-run`, `--yes`, unchanged replay and `site:doctor --strict` processes, asserting every governance artifact (permission catalogue, discoverable field-sealing policy, governance provider, workflow definition and assignment, all five companion tests including the JSON:API companion) is planned and published byte-identical to the golden emitter fixtures, bound to the plan digest and the approval receipt, with closed `.waaseyaa/generated.json` evidence, a byte-identical replay snapshot, and a read-only green doctor that creates no database; the envelopes are pinned as `complete-planned.json`, `complete-applied.json` and `complete-no-changes.json` beside the unchanged minimal and refusal fixtures. That flow exposed a real defect: strict doctor recompiles from the published `.waaseyaa/site.yaml`, whose canonical key order re-parses fixture values and relationship lists differently from the authored YAML, and `GovernanceCheckEmitter`/`EntityClassEmitter` iterated parse order, so an untouched complete project reported `SITE010_GENERATED_ARTIFACT_DRIFT`. Both emitters now sort fixture values and relationships by id before rendering (digests were already canonical; only bytes drifted). `docs/change-records/FW-SITE-BLUEPRINT-01.md` is corrected to the accepted behaviour — a workflow-bound generated entity emits the `workflow_state` selector only, never a fabricated `status` field — and records the shared permission-catalogue authority (G1 closed), the real-controller JSON:API companion, the field-level authorization-input seal, and the emitter purity decision; `docs/specs/site-golden-path.md` and `docs/specs/access-control.md` carry the same contract. Packaged HTTP/admin acceptance and fixture materialization remain #2789 and later work.
