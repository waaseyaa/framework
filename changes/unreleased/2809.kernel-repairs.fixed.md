- **Fixed — kernel entity-manager composition now requires a compiled package manifest:** the boot-scoped field-type authority is never constructed from an uninitialized or partial kernel lifecycle. Kernel-path integration fixtures execute manifest compilation before entity-manager composition, and the attachment/AI CLI integration callers pass the authority explicitly.
