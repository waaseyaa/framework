- **Fixed — `sync-rules` in direct-package consumers (#2832):** the CLI now reads canonical rules from the loaded Foundation package instead of an absent `waaseyaa/framework` directory. Aggregate installs and independently located Composer path packages use the same source; dry-run and overwrite behavior are preserved.
