- Added the pure artifact-plan value types the generation authority compiles into: `ArtifactPlan` (`waaseyaa.artifact_plan` v1) with the canonical plan digest, `ProjectStateIdentity` (`waaseyaa.project_state` v1) with its captured targets, `ComposerProviderRegistration`, and the four closed vocabularies they serialize. They are pure values that observe no project and are reachable from no command; the engine that consumes them lands in a later slice (#2846, ADR-025 D-6).
