- **Added — one canonical distribution-exclusion policy now governs Docker build contexts, deploy artifacts assembled with rsync, and Git/Composer source archives (#2648):** `support/distribution-exclusion-policy-v1.json` declares surface-specific exclusions for generated `.env` files and local Composer overrides, machine MCP descriptors, local agent adapters, storage/operator state, and explicit `.env.example` archive readmissions. The approved-docs guard rejects `docs/`, `docs/**`, and wildcard Markdown export rules even when appended outside the managed block. `bin/check-distribution-exclusion` renders and verifies `.gitattributes` and `skeleton/.dockerignore`; every workflow containing `rsync` must carry every policy-declared exclusion in anchored form, with independent omission and mutation controls for each required argument while the existing unanchored-`docs/` sentinel remains live. `--self-test` copies all mutation targets into a unique temporary tree, so it never rewrites tracked surfaces, then executes and inspects both `git archive` (the #2649 sealing path) and Composer's own `composer archive` against isolated repositories; both must omit representative governed files while retaining approved specs and `.env.example` templates. `skeleton/bin/maintenance/verify-deploy-rsync` delegates in deploy-only mode through either the monorepo or installed `vendor/waaseyaa/framework` gate, with the historical docs-only fallback limited to trees where that gate is unavailable. `tests/Architecture/DistributionExclusionGateTest.php` binds the policy to CI/preflight and pins the security-sensitive sets independently of the renderer. `bin/check-skeleton-docker-secret-exclusion` (#2647) remains the Docker-daemon proof for generated secrets, and framework dist size allowlists remain **#2650**.
