- **Fixed — JSON:API committed-outcome phase signal (#2999, API/entity-storage slice):** `UnitOfWork` keeps throwing per-outer-transaction token-stamped `TransactionCompletionException` after commit (token rotates each outer `transaction()`, stable for nested calls); `EntityRepository` translates only a matching-token completion into `EntityMutationCommittedSideEffectsFailedException`; `JsonApiController` maps that repository signal to HTTP 500 `COMMITTED_SIDE_EFFECTS_FAILED`. Independent nested writes during PRE_SAVE/PRE_DELETE cannot produce a false committed response. MCP mapping remains residual.
