- **Added — DORA input-readiness audit (#2869):** records the actual Framework publication and consumer production-promotion sources, maps them to the four DORA metrics with present-versus-missing evidence, and proposes a first dashboard that does not treat PR merges as deployments. The promotion source is the pinned `waaseyaa-infra` workflow and run; its `revision` is an infra checkout/promotion input, and no consumer/framework pin mapping is claimed without pinned source evidence. The workflow requests the `production` environment, but the observed API configuration had no protection rules or deployment branch policy.
