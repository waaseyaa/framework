- **Changed — ADR-025 names the single generation-authority owner (#2845):** the CLI's twenty-plus `make:*`/`scaffold:*`/`fixture:*`/`site:init` generation entrypoints were built without one authority for transactional safety, and `make:content-type` mutates `composer.json` directly with no lock, collision check, or rollback. `docs/adr/025-unified-artifact-generation-authority.md`, with its command inventory at `docs/adr/data/025-generation-command-inventory.json`, decides that `Waaseyaa\CLI\Site\SiteInitializationService` is the single transactional execution authority and `.waaseyaa/generated.json` (via `GeneratedSite`) is the single generated-state authority for every generator — human scaffold, `site:init` preset, or approved blueprint alike — and that #2664, #2846, and #2787 each compose it rather than build a competing one. This is a decision record only: no generator, handler, or command behavior changes; migration of the five `merge`-disposition commands is sequenced as follow-on issues in the ADR's D-11.
