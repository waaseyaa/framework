- Corrected the recipe-activation contract for the supported full-framework skeleton: its recipe packages are already installed by the framework dependency graph, while fixed typed registrations in the root artifact plan are required to activate providers through literal `composer.json` (#2857, ADR-025 D-15). A Composer materialization phase remains deferred until a supported thinner consumer and load-bearing packaged proof are explicitly accepted.
