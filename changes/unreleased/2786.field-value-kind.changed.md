- **Changed — downstream field types can declare a GraphQL-safe value shape (#2786):** field plugins now expose a transport-neutral `FieldValueKind` that GraphQL adapts without creating a field-to-GraphQL dependency. Existing field types preserve their prior mappings, while an undeclared kind fails closed instead of silently becoming `String`.
