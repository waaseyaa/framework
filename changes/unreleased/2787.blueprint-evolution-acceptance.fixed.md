- **Fixed — discriminating acceptance evidence for the blueprint additive-successor path (#2787, FW-SITE-BLUEPRINT-01D):** `ApplicationBlueprintCompiler` and `SiteInitializationService` already compiled and admitted a governed additive successor blueprint (`minimal.yaml` → `complete.yaml`) through the existing `set_evolution: additive` engine gate, but no test exercised applying two *different* blueprint manifests sequentially against the same project root. `BlueprintExecutionAdmissionTest` now proves, against a literal reviewed added-path set (not a self-derived oracle), that the successor's own compiled plan and the engine's `EvaluatedArtifactPlan::$adds`/`$drops` agree exactly: every `minimal.yaml` path survives, the eleven `complete.yaml`-only paths (the `person` entity plus the governance emitters minimal.yaml's empty sections never trigger) are exactly what is added, the dry-run preview writes nothing, publication carries the fresh approval's decision receipt as evidence, and a replay is idempotent. Two adversarial companions close the boundary: hand-editing an already-published managed artifact before the successor attempt refuses with a collision (`SiteInitializationCollisionException`, "edited outside an extension region") leaving the project byte-identical and no new path present; and injecting a fault mid-publication during the successor apply rolls back to the prior approved snapshot exactly, with receipt A remaining the durable approved evidence, receipt B attributed only to the terminal failure receipt, and no transaction journal or stage residue left behind. `SiteBlueprintProcessTest` mirrors the happy path through the real `bin/waaseyaa site:init` console process (apply `minimal.yaml` under receipt A, then preview/apply/replay `complete.yaml` under a fresh receipt B), pinned against new `changed-planned.json`/`changed-applied.json`/`changed-no-changes.json` golden JSON envelopes. No production behavior changed; this closes an acceptance-coverage gap the #2787/#2788 blueprint work left open. #2664 orchestration and #2857 activation remain separately tracked and out of this slice's scope.
