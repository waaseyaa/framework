- **Added — expected project-config authoring identity (#3042):** `project:config:authorize` can require paired canonical site-manifest and site-plan digests, refusing malformed inputs before signing and mismatches before success output while preserving legacy output when omitted.
