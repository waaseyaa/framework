- **Fixed — production-install bounded helper lost descendant process-group custody (#3064):** Independent re-review of `f0794db` reproduced that `run_bounded` cleared owned PID/PGID after the direct leader exited 0, so a backgrounded `sleep` descendant survived EXIT cleanup. The helper now isolates jobs under bash monitor mode with a verified leader PGID (never the caller's group, no racing `ps` adoption), reaps surviving group members after every leader exit including success, and clears custody only after the owned group is gone. Architecture coverage exercises the exact early-parent-exit descendant case and a TERM-ignoring timeout→KILL case with PIDs recorded outside the removed work tree. Production code is unchanged.
