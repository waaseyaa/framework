- **Changed: `waaseyaa/admin-surface` now has a committed package charter, a concern-separated canonical TypeScript wire contract, exact SPA-mirror compatibility, and a scoped Deptrac 4 architecture authority (#3074, #3075).** The package documents its HTTP, wire, composition, public, split, and dependency boundaries; core, schema, revision, and page-builder contracts now reflect emitted nullability and the `mutation_token` protocol; a newer null mutation token clears stale client-side mutation authority; all production PHP tokens are assigned to Boundary contract, Application adapters, or Delivery and composition; uncovered, forbidden, and TypeScript contract drift fail through discriminating controls; and a Deptrac-generated Mermaid view records the observed graph. The existing repository-wide package-layer gate remains active until #3075 proves and reviews full migration parity.
