- Withdraw the extension invitation from `Waaseyaa\Ingestion\EnvelopeValidator`. Its docblock told applications to extend it while the class carried `@internal` and `packages/ingestion/public-surface.php` declared it `internal`; the contradiction is resolved by removing the invitation, and consumers wanting a supported foreign-records-to-entities path are pointed at the migration substrate's public plugin contracts (ADR-012a). The class, its signatures and its disposition are unchanged and it remains shipped — `waaseyaa/ingestion` is published, so any removal would be a separate decision under the stability charter's deprecation cycle. Documentation only; no behaviour changes.
