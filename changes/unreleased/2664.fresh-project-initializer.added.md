- **Added — fresh-project initialization (#2664, FW-PROJECT-INITIALIZER-01):** `project:init` composes the existing `site:init` and `install:init` phases through the installed CLI without booting the parent. Dry-run stops before installation; failed site execution or unconfirmed child cleanup prevents the second phase. Verification remains a separate `composer site-verify` step. Upgrade, AI generated-state verification, and output removal remain separate work.
