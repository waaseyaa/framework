- **Fixed — manual and blueprint governance generation now converge on production-enforced artifacts (#2848):** `make:policy` and `scaffold:workflow` render canonical policy and workflow definitions through the same emitters used by `application_blueprint`. Blueprint compilation emits `config/sync/workflows.assignments.yml` as a writable CFG-03 artifact and adds a reserved boolean `status` field to workflow-bound revisionable entities; that field is column-stored, defaults to `false`, is `Protected`, and participates in authorization input. The packaged harness installs an archived candidate as real package copies, signs generated config through separate authoring custody, imports only the verifiable envelope, and boots the production kernel. Its transition probe requires denied publication to leave the full base and revision rows unchanged, then requires allowed publication to persist `status = 1`, `workflow_state = published`, `vid = 2`, the matching published pointer, and internal revision ids `[1, 2]`. The Community Events packaged suite passed 45 tests / 135 assertions plus real reference-existence writes; an explicit signed-import `ConsoleKernel` proof also passed draft to review to published with configured `vid` and Organizer/Venue references. The current generated-application journey still requires that explicit signed import: Framework #3037 owns canonical phase-aware activation/retry. This candidate does not claim zero-touch readiness, publication, deployment, or completion of Framework #2981. See `FW-GOVERNANCE-PACKAGED-ACCEPTANCE-01` for exact evidence and remaining gates.
