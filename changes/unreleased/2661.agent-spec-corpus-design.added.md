- **Added — design contract for a versioned, sanitized agent specification corpus (#2661):** `docs/specs/agent-spec-corpus.md` keeps `docs/specs/` authoritative while defining a closed lifecycle catalog and manifest, reciprocal supersession links, a physically live-only default index, deterministic line-preserving sanitization/chunking, exact digest/version preimages, current-state compiler validation with candidate-diff lifecycle review, atomic publication recovery, and the package boundary handed to #2662 for later offline cited search. No search surface, adapter, MCP route, or runtime activation is added by this design slice.
