- **Fixed — blueprint `workflows.assignments` emission is CFG-03 importable (#2848):** `WorkflowDefinitionEmitter::renderAssignments()` now builds the artifact through `ConfigSyncFile::writable()` and `ConfigSyncSerializer::toYaml()`, deriving schema identity from the guarded `WorkflowAssignmentsConfig::register()` registration rather than hand-typed `_meta` or bare mapping YAML. Golden blueprint and `SiteBlueprintProcess` fixtures were regenerated from the canonical test flow on the clean baseline.
