- Future `substantive_review_issued` and `repair_completed` delivery-agent batch events must derive `elapsed_ms` from an authoritative, identity-matched, causally-ordered start event (`review_started` / `repair_started`) with explicit `occurred_at` timestamps on both ends; the frozen v1 ledger and its historical null/legacy values are unaffected.
