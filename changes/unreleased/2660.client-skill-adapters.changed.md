- **Changed — Codex and Claude now receive equivalent per-skill delivery with concise always-loaded guidance (#2660 Part B):** `ClientCapabilityRegistry` moves `codex` to `PerSkillFile` with `.agents/skills/waaseyaa-<id>/SKILL.md` plus concise root `AGENTS.md`; Claude keeps `.claude/skills/waaseyaa-<id>/SKILL.md` and marker-bounded `.claude/CLAUDE-WAASEYAA.md`. Shared rendering lives in `AbstractPerSkillClientTransformer`. Canonical skill source sha256 values are recorded in `ParsedSkill`, `SkillInventory`, and generated provenance markers. `ClientCapabilityDiagnostics` emits deterministic warnings for unsupported capabilities; `bimaaji:install --verify` checks drift without writing.
