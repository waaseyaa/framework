- **Added — exact landing-base prequalification (#2525, `FW-DELIVERY-LANDING-PREFLIGHT-01`):** `bin/check-landing-base` consumes a machine-readable declaration binding the recorded base, current base, head, and strict unique range to exact commits, then reports real changed paths, contract-input changes, overlap, behind/ahead counts, generated-output conflicts, and Git's predicted merge tree before expensive qualification. Shallow, disconnected, ambiguous, dirty, moved-ref, and invalid-range evidence fails closed; harmless base advancement remains clean, and every result explicitly denies qualification and merge authority. Hosted handoff and landing-queue acceptance remain deferred under the open issue.
