- **Added — safe registered-role account provisioning (#3046):** `user:provision-registered` accepts a bounded canonical stdin request, creates the account with its selected application role and canonical permission union in one repository save, returns closed retry/uncertainty outcomes, and never accepts credential bytes in argv or environment configuration.
