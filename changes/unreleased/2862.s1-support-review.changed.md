- **Changed — the S1 support contract records its 2026-09-03 review (#2862):** `support/s1-v1.json` advances `last_reviewed` to `2026-09-03` and `review_by` to `2026-10-15`. No platform row changed: PHP 8.5's active-support (`2027-12-31`) and security-support (`2029-12-31`) ends, Node 24's maintenance start (`2026-10-20`) and EOL (`2028-04-30`), Ubuntu 24.04's standard security maintenance end (`2029-05`), the Composer 2.10 feature line, the SQLite constraint satisfied by noble's `3.45.1`, and the Playwright `1.60.0` pin recorded by `packages/admin/package-lock.json` were each re-checked against their upstream primary source and all match what the contract already asserted. `review_by` is set to `2026-10-15` rather than the 93-day maximum of `2026-12-05` so that the next review falls **before** Node 24 enters maintenance on `2026-10-20`; that transition is a support reduction the 90-day notice guard cannot currently see, because `bin/check-support-contract:195` builds its notice set from `platform.php.active_support_end` and `platform.node.eol` only while `platform.node.maintenance_start` is date-validated at `:129` and then unused. October 15 is a bounded compensating review before the transition, not retroactive notice compliance — the 90-day window for `2026-10-20` has already passed. Converging every declared support-reducing transition on one interpretation is #2852's work.
