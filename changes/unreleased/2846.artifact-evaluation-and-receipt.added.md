- Added the evaluation and apply half of the generation contract: `EvaluatedArtifactPlan` pairs one plan with the project state it was evaluated against, `ArtifactApplyRequest` carries the reviewed plan bytes to apply, `ArtifactApplyResult` is the versioned result envelope, and `ChangeReceipt` is the governed-change envelope the generation binding emits and does not retain. Target evaluation moved out of the site initialization planner into one shared evaluator so preview and apply cannot diverge; no command reaches the new surface yet (#2846, ADR-025 D-6.2, D-6.4, D-6.5, D-14.7).
