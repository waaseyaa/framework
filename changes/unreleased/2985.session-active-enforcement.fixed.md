- **Fixed — unconditional active-account admission in SessionMiddleware (#2985, F9):** disabled persisted users are no longer authenticated on the existing-session or generic bearer paths when optional `waaseyaa/auth` eligibility is absent; verified-email policy remains optional; missing `UserInternalFieldReaderInterface` fails closed on both paths.
