- Activate reviewed-plan and project-state checks for controlled generation apply, transient change receipts and opt-in `site:init --json`, with unit-aware `site:doctor`. Stale or unverifiable requests refuse GEN005 before staging. Fresh cancellation and rollback leave no control-ignore artifact because it is now journalled with the reviewed files; successful default output and generated bytes remain compatible. Other compiler migrations remain closed.
