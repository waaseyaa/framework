- **Added — configurable host-bound session and CSRF cookies (#3047):** `session.cookie` now owns session/CSRF names plus path/domain, with a `__Host-` profile that forces Secure, `Path=/`, and no Domain while Admin SPA request/upload readers share one CSRF cookie decoder (#3031).
