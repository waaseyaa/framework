- Registers `site:apply --decision-receipt` with the same closed read-once `SITE050` receipt decoding as `site:init`, shared via `DecisionReceiptInput`, and passes the approval separately to `SiteInitializationService::apply()` so reviewed blueprint plans bind across the later process.
