- Adds a real-process acceptance control for preserving reviewed plan, current project state, and separate exact-digest blueprint approval across `site:init --dry-run` and later `site:apply`; the control currently records the missing `site:apply --decision-receipt` blocker.
