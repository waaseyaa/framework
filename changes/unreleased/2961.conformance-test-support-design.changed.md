- **Recorded the packaged-consumer reachability finding and the design for the
  remaining conformance helpers.** `docs/change-records/FW-CONSUMER-TEST-SUPPORT-01.md`
  states the problem (Composer honours `autoload-dev` for the root package only,
  so `Waaseyaa\Migration\Testing\{Destination,Source}ConformanceTestCase` and
  `Waaseyaa\Entity\Testing\Translation\TranslatableEntityContractTest` — all three
  declared `public` — cannot be autoloaded by a consumer), the layer and
  precedent constraints, and three options: flipping each package's own mapping
  from `autoload-dev` to `autoload`, dedicated test-support packages carrying the
  existing namespaces, and the simpler entity-only placement in `waaseyaa/testing`
  (assessed and rejected as dominated). It records the acceptance evidence any
  repair must produce — a downstream conformance case that really passes, one
  that deliberately fails, and `--no-dev` boot evidence — and explicitly proposes
  no scanner change. `docs/audits/cleanup-backlog.md` gains CL-14 through CL-19
  covering the audit-data, documentation, tooling-drift and packaging findings
  from the same review. Documentation only; no behaviour changes.
