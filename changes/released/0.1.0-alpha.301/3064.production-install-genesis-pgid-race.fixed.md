- **Fixed — production-install bounded helper raced on post-spawn /proc PGID adoption (#3064):** Independent re-review of `b0c2e6d` showed `run_bounded` could refuse with status 126 when a fast `/bin/true` leader exited before `/proc` observation, and that delaying the observer after an early-exiting parent cleared custody and leaked a backgrounded `sleep` descendant. Custody now records the monitor-mode leader PID as the owned PGID immediately (refusing only caller-group equivalence), never abandons the group because the leader disappeared, and Architecture coverage repeats fast `/bin/true`, the delayed-observer early-parent leak discriminator, and a PHP_BINARY TERM→KILL marker (no Perl, no brittle ≥5s floor). Production code is unchanged.
