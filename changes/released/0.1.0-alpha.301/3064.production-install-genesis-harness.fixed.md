- **Fixed — production install genesis packaged harness lacked per-child deadlines (#3064):** Independent review of `06d45e45` accepted the runtime discovery/capability fix and exact-head archived-consumer semantics, but rejected the harness because Composer/CLI/probe children had no bounded deadline and EXIT cleanup did not reap a stalled process group before `rm -rf`. The harness now sources `tests/PackagedForm/lib/run-bounded.sh` (setsid + `timeout --signal=TERM --kill-after=5s`, deterministic status 124 diagnostics) for every child, reaps before tree removal, and Architecture coverage proves the deadline/termination contract including a stalled-child discriminator. Production code is unchanged.
