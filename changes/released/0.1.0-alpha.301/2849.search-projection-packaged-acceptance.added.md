- Add `tests/PackagedForm/check-search-projection-scaffold-acceptance`, which drives the whole `make:search-projection` flow from a disposable consumer built out of the exact candidate tree with its own resolved dependencies: `site:init`, `install:init`, `make:content-type`, a deliberate per-field visibility decision, `make:search-projection`, `schema:sync`, `entity:create`, then `search:reindex`. It asserts the generated provider is registered exactly once in literal root `composer.json`, that reindex indexes one document — which only succeeds if the generated projector was discovered and consulted ahead of the built-in default — that the deliberately-public field's text is present in the stored index, and that the field left at the restricted default is absent from it. A negative control removes only the literal-root registration, leaving the generated classes byte-identical on disk, and requires reindex to index zero documents.
