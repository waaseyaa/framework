- **Fixed — ADR-025 D-8 cited the wrong renderer return type (#2845, ADR-025 D-8):** the fan-in argument opened by attributing `SiteRecipeRendererInterface::render(SiteManifest): GeneratedSite` to the per-recipe interface. The interface returns `list<GeneratedArtifact>` (`packages/site-contract/src/Generation/SiteRecipeRendererInterface.php:14-15`); `SiteArtifactRenderer::render()` (`SiteArtifactRenderer.php:30`) is the composer above the recipes and is what returns `GeneratedSite`. The argument the section makes is unaffected, but the ADR becomes accepted evidence for #2846, #2787 and #2664, so the citation is corrected before acceptance rather than after.
