- **Fixed — preserve installed CFG-03 contracts across package-manifest cache hits (#3044):** root metadata remerge now retains the exact cached configuration-contract cohort so later processes can verify authored configuration against the packages actually installed.
