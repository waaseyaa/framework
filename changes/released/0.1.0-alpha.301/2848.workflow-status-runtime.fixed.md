- **Fixed — compiled workflow entities support durable publication (#2848):** workflow-bound entities declare the protected boolean status field required by the production workflow save guard. The packaged governance probe now asserts complete row preservation on denied transitions and persisted publication state/revision pointers on success, using probe bytes from the same immutable candidate archive. Separate signed configuration import remains an explicit prerequisite; #3037 owns automatic fresh-application activation. See `FW-GOVERNANCE-PACKAGED-ACCEPTANCE-01` for source and consumer evidence.
