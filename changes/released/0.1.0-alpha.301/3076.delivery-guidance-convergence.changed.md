- **Changed - agent delivery guidance now selects a host-compatible Git entrypoint (#3076, `FW-DELIVERY-GUIDANCE-CONVERGENCE-01`):** supported POSIX hosts continue through `bin/git`, native Windows work may use an explicitly selected Windows Git executable, and both retain the ban on `git stash`. The workflow also states that already-authorized delivery continues past planning while independent review does not grant multi-agent implementation authority.
