- Local delivery-ledger checks compare against the branch merge-base, avoiding false failures after unrelated main appends. CI validates immutable candidate history, and governed merges pin the head under strict required checks.
