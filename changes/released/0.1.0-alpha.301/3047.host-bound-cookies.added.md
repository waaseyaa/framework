- **Added — configurable host-bound session and CSRF cookies (#3047):** `session.cookie` now owns session/CSRF names plus path/domain, with a `__Host-` profile that forces Secure, `Path=/`, and no Domain while Admin SPA request/upload readers share one CSRF cookie decoder (#3031). Malformed cookie config is rejected at construction (including `;` in path/domain); configured session names resume on otherwise-stateless paths; prestarted sessions are checked against the full effective Secure/HttpOnly/SameSite policy; every packaged Admin HTML response (SPA fallback and direct `.html` assets) rewrites `csrfCookieName` from the runtime policy with literal-safe replacement.
