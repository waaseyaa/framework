- Add dormant additive successor-plan evaluation for the managed manifest-render root compiler, with sorted set deltas, closed eligibility and unchanged ownership protections. Other compilers and all live entrypoints remain frozen until the activation slice.
