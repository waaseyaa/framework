- **Added — fail-closed generator-feature negotiation and the pure blueprint root compiler (#2787, FW-SITE-BLUEPRINT-01D, slice 01D-1):** `site:init` no longer silently ignores an `application_blueprint` section no active compiler understands. `Waaseyaa\SiteContract\Generation\GeneratorFeatureNegotiation::assert()` refuses `GEN007_UNSUPPORTED_DECLARATION` at `/application_blueprint` immediately after manifest parsing and before every render, lock, journal or write — identically in `--dry-run` and `--yes`, and in text and `--json` — whenever `SiteManifest::$requiredGeneratorFeatures` names a token `SiteArtifactRendererFactory::advertisedGeneratorFeatures()` does not advertise; that roster is `[]` in this slice, so a blueprint-bearing manifest is refused regardless of the installed recipe set, while a blueprint-free manifest's bytes, exit codes and envelopes are unchanged. `Waaseyaa\CLI\Site\Blueprint\ApplicationBlueprintCompiler` is a new, pure root compiler composing an injected `SiteArtifactRenderer`'s manifest rendering with a fixed, ordered emitter roster (`EntityClassEmitter`, `RelationshipEmitter`, `ProviderRegistrationEmitter`) behind the new `Waaseyaa\CLI\Site\Blueprint\Emitter\BlueprintArtifactEmitterInterface` seam; it declares its own `generator.fqcn`, the existing root `site` unit, and `set_evolution: additive` purely, leaving eligibility entirely to the execution authority. **Engine admission is explicitly not widened in this slice**: `SiteInitializationService::ADDITIVE_COMPILERS` stays `[SiteArtifactRenderer::class]`, and no handler, handler-reachable factory or doctor path constructs the compiler — a compiler plan is refused `GEN011_UNAUTHORIZED_SET_DELTA` on evaluation, on a fresh or an already-initialized project, before any write. A follow-on slice adds the compiler to the closed ADR-025 D-13 eligibility list, a `--decision-receipt` input verified inside engine evaluation, and the `application_blueprint` evidence member of `.waaseyaa/generated.json`.
