- **Fixed — blueprint governance: JSON:API companion proof and field-level sealing of authorization inputs (#2788, FW-SITE-BLUEPRINT-01E review gaps 2 and 3):** `GovernanceCheckEmitter` now also emits `tests/Blueprint/JsonApiGovernanceChecksTest.php` whenever a blueprint declares a policy. It drives every blueprint entity through the real `Waaseyaa\Api\JsonApiController` — `store()`, `index()`, `show()`, `update()`, `destroy()` — composed with a real `EntityAccessHandler` over the generated policy, a `TemporarySqliteDatabase`-backed repository, and immutable principals, asserting the exact status and document shape for a permission-less principal (and anonymous on `show`: the canonical `404` not-found shape, never an existence oracle), for an allowed principal wherever the blueprint declares a policy for that operation, and for near-miss principals (holding the permission but not the owner, or outside the listed workflow states). `AccessPolicyEmitter` now makes the generated policy implement `FieldAccessPolicyInterface` whenever the entity has an authorization input: the `keys.owner` field is edit-Forbidden once the entity is persisted (`isNew()` is the lifecycle authority; create-time authorship stays settable, the `NodeAccessPolicy` precedent) and the engine-owned `workflow_state` selector is edit-Forbidden always, so an entity-level `update` grant can no longer reassign ownership or move workflow state through the API. The `complete.yaml` reference fixture gains `article_create` (permission) and `article_delete_own` (ownership) policies so every JSON:API operation has both allowed and denied proof; its pinned digests and golden artifacts are re-recorded. Architecture registrations exposed by the earlier full-suite run are repaired without weakening the gates: `AuthorizationInputReader` joins the closed raw-authority roster, the six blueprint emitters are recorded as reviewed compile-time refusal sites, `GovernanceCheckEmitter` is classified `tooling-generator` in the S1 schema-authority roster, and the companion-test child process now runs through Symfony Process instead of a raw `proc_open()`.
