- Add a commit-bound exact-set projection for governed delivery-agent events
  and a tracked Grafana dashboard that exposes projection identity while
  preserving pending adjudications without reinterpreting them as unresolved.
