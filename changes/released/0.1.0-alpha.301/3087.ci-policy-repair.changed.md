- Changed the governed CI policy contract to bind every producer and required context to the generated workflow inventory, replacing the unbindable environment-setup producer with the two shard-plan jobs, binding source-integrity policy and release-publish evidence to the jobs that implement them, correcting the shard matrix axis and the random-order aggregate lineage, and defining release cadence as a tag push.
