- **Fixed — production `install:init` required an active CFG-02 generation before it could create genesis (#3064):** After `APP_ENV=production db:init --no-sync-schema`, restricted discovery still ran `CapabilityRegistry::validate()`, and `ConfigurationAuthorityServiceProvider::capabilityDeclarations()` called `requireActiveGenerationId()` — recreating the #2428 circularity at the capability layer. Definition-only registration (`bootForSchemaSync` / `restrictedDiscoveryOnly`) now skips live capability validation while ordinary production boot still refuses without an active generation. Packaged coverage proves the exact production sequence, one genuine genesis, idempotent replay, pre-install refusal, and post-install boot/read. `APP_ENV=local` is not used as installation authority.
