- **Fixed — queue README named a non-existent `JobInterface` (#2822):** the package told consumers it provided `JobInterface` and listed that name as a key class, but no such type exists. Application jobs subclass public abstract `Job` and implement `handle()`. README key classes now name only loadable types (`Job`, `QueueInterface`). Job middleware remains foundation `JobMiddlewareInterface`. `QueueJobContractSurfaceTest` pins the README names and the public disposition so the mismatch cannot recur.
