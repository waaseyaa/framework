- **Added — read-only consumer-promotion adapter (#2869):** `php bin/adapt-consumer-promotions` maps dumped `promote.yml` `workflow_dispatch` runs to production-promotion records, rejects PR merges and push deploys, and counts only successful promotions as DORA deployment-frequency samples.
