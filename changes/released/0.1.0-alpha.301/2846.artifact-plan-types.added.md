- Added the pure artifact-plan value types the generation authority compiles into: `ArtifactPlan` (`waaseyaa.artifact_plan` v1) with the canonical plan digest, `ProjectStateIdentity` (`waaseyaa.project_state` v1) with its canonical digest and internally consistent captured targets, `ComposerProviderRegistration`, and the four closed vocabularies they serialize. ADR-025 now fixes both digest formulas and records that slice 2 serializes `set_evolution` while slice 7 enforces its eligibility. The types observe no project and are reachable from no command; the engine that consumes them lands in a later slice (#2846, ADR-025 D-6).
