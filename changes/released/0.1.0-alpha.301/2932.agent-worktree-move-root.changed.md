- **Agent worktree relocation no longer rediscovers a silent fetch abort.**
  `docs/governance/agent-contract.md` records the remote-first sequence for
  harness root moves (Cursor `move_agent_to_root` and equivalents): create the
  worktree on an already-published tip, relocate, then branch — or run `bin/git push -u origin HEAD`
  before relocating — and install worktree `vendor/` before the first push when
  pre-push preflight requires it. `AGENTS.md` points adapters at that contract
  section. Documentation only; no runtime behaviour change.
