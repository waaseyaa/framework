- **Added — read-only `ai:verify` CLI for Bimaaji generated-state checks (#2664, `FW-AI-VERIFY-01`):** `bin/waaseyaa ai:verify` verifies schema 1 `.waaseyaa/bimaaji-install.json` ownership records against on-disk bytes and the current transformer render set without writing anything. `InstalledManifest::readStrict()` reports missing, unreadable, malformed, and unsupported-schema manifests distinctly while `InstalledManifest::load()` stays fail-soft for `bimaaji:install`. Verification separates wholefile sha1 provenance from managed-region freshness; bytes outside `ManagedRegion` markers may differ without failing when the managed region still matches the current render. `--json` emits a bounded deterministic machine report. `ai:update`, upgrade migrations, Composer hooks, and `.waaseyaa/generated.json` reconciliation remain #2664 residual scope.
