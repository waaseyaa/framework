name: Admin SPA Distribution Build

on:
  push:
    branches: [main]
    paths:
      - 'packages/admin/**'
      - '.github/workflows/admin-dist.yml'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  actions: write

concurrency:
  group: admin-dist
  cancel-in-progress: true

jobs:
  build-and-publish:
    name: Build admin SPA and open dist PR
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          # Default token: the push it makes won't trigger CI on the branch
          # (GITHUB_TOKEN trigger suppression), so CI is chain-dispatched
          # explicitly after the PR is created/updated below. RELEASE_PAT is
          # abandoned (#1937). (#1934)
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '25'
          cache: 'npm'
          cache-dependency-path: packages/admin/package-lock.json

      - name: Set up PHP
        uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2.37.2
        with:
          php-version: '8.5'

      # Single reproducible step (npm ci + generate + copy + freshness signature)
      # so the committed dist and packages/admin-surface/dist.signature stay in
      # lockstep with the admin source (D6 / check-admin-dist-fresh gate).
      - name: Build admin SPA dist (+ freshness signature)
        run: bin/build-admin-dist

      - name: Check for changes
        id: diff
        run: |
          git add packages/admin-surface/dist/ packages/admin-surface/dist.signature
          if git diff --cached --quiet packages/admin-surface/dist/ packages/admin-surface/dist.signature; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -B admin-dist/update
          git commit -m "chore(admin-surface): update pre-built SPA dist

          Built from $(git rev-parse --short HEAD) by admin-dist workflow.

          Co-Authored-By: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
          git push --force-with-lease origin admin-dist/update

      - name: Create or update PR
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          EXISTING_PR=$(gh pr list --head admin-dist/update --json number --jq '.[0].number // empty')
          if [ -n "$EXISTING_PR" ]; then
            echo "PR #${EXISTING_PR} already exists — push updated it."
          else
            gh pr create \
              --title "chore(admin-surface): update pre-built SPA dist" \
              --body "## Summary

          - Rebuilt Nuxt 4 admin SPA from latest \`packages/admin/\` source
          - Updated \`packages/admin-surface/dist/\` with production build output
          - Assets ship via Composer so \`create-project\` installs work without Node.js

          ## Test plan

          - [ ] Verify \`packages/admin-surface/dist/index.html\` exists and is valid HTML
          - [ ] \`composer create-project\` install serves \`/admin\` from vendor dist
          - [ ] \`/admin/_surface/*\` API endpoints still work

          Automated by \`admin-dist.yml\` workflow." \
              --head admin-dist/update \
              --base main
          fi

      - name: Chain-dispatch CI on the dist branch
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # The bot-token push above does not trigger CI on the PR, but
          # workflow_dispatch events are exempt from GITHUB_TOKEN trigger
          # suppression, so dispatching ci.yml on the branch restores the
          # PR's checks without a PAT. (#1937)
          gh workflow run ci.yml --repo "$GITHUB_REPOSITORY" --ref admin-dist/update

      - name: Skip — no changes
        if: steps.diff.outputs.changed == 'false'
        run: echo "Admin SPA dist is already up to date."
