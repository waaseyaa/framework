{
  "mcpServers": {},
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/bin/project-hooks",
            "args": ["claude-session"],
            "timeout": 5,
            "statusMessage": "Loading concise branch context..."
          }
        ]
      }
    ]
  }
}
